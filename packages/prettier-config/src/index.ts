import type { Config } from "prettier";

const config: Config = {
	printWidth: 80,
	useTabs: true,
	tabWidth: 2,
	semi: true,
	trailingComma: "es5",
	bracketSpacing: true,
	arrowParens: "always",
	endOfLine: "lf",
};

export = config;
